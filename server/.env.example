PORT=5001

DB_USER=postgres
DB_HOST=localhost
DB_NAME=marketplace
DB_PASSWORD=password
DB_PORT=5432

JWT_SECRET=change_me_for_local_development

# Optional email delivery settings.
RESEND_API_KEY=
EMAIL_FROM=

# 32-byte (64 hex chars) key for AES-256-GCM payment card encryption.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CARD_ENCRYPTION_KEY=change_me_generate_a_real_64_hex_character_key_for_local_dev
